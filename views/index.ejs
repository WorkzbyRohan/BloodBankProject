<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blood Bank System - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Montserrat', sans-serif; }
    .fade-in { animation: fadeIn 1.2s ease; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(30px);} to { opacity: 1; transform: none; } }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-100 via-red-200 to-gray-300 text-gray-900 min-h-screen flex flex-col">
  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-red-800 via-red-700 to-red-600 text-white p-4 flex justify-between items-center shadow-lg sticky top-0 z-10">
    <div class="text-2xl font-extrabold tracking-wide flex items-center gap-2">
      <span class="inline-block bg-white bg-opacity-10 rounded-full p-1"><svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 8c-1.657 0-3 1.343-3 3 0 2.5 3 5 3 5s3-2.5 3-5c0-1.657-1.343-3-3-3z"/><circle cx="12" cy="11" r="3"/></svg></span>BloodLink
    </div>
    <div class="space-x-6 text-lg">
      <a href="/index" class="underline font-bold">Login</a>
      <a href="/signup" class="hover:underline transition">Sign Up</a>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-red-900 via-red-700 to-gray-200 py-12 flex flex-col items-center justify-center text-center mb-8 fade-in">
    <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1200&q=80" alt="Blood Donation" class="absolute inset-0 w-full h-full object-cover opacity-20 pointer-events-none" />
    <div class="relative z-10">
      <h1 class="text-5xl md:text-6xl font-extrabold mb-4 text-red-100 drop-shadow animate-pulse">Blood Bank System</h1>
      <p class="text-lg md:text-2xl text-gray-100 mb-6 max-w-2xl mx-auto">Login to access your dashboard and <span class="text-red-300 font-bold">help save lives</span>.</p>
    </div>
  </section>

  <!-- Login Form -->
  <main class="flex-1 flex flex-col items-center justify-center">
    <div class="bg-white bg-opacity-95 p-10 rounded-3xl shadow-2xl w-full max-w-md fade-in border border-red-200">
      <h2 class="text-3xl font-extrabold mb-6 text-center text-red-800">Login</h2>
      <form onsubmit="return loginRedirect()" class="space-y-5">
        <div>
          <label for="role" class="block font-semibold mb-1">Login Role</label>
          <select id="role" required class="w-full border-2 border-red-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-600 bg-gray-50">
            <option value="">Select Role</option>
            <option value="admin">Admin</option>
            <option value="patient">Patient</option>
            <option value="donor">Donor</option>
          </select>
        </div>
        <div>
          <label for="username" class="block font-semibold mb-1">Username</label>
          <input type="text" id="username" placeholder="Enter Username" required class="w-full border-2 border-red-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-600 bg-gray-50">
        </div>
        <div>
          <label for="password" class="block font-semibold mb-1">Password</label>
          <div class="relative">
            <input type="password" id="password" placeholder="Enter Password" required class="w-full border-2 border-red-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-600 pr-10 bg-gray-50">
            <span class="absolute right-3 top-2.5 cursor-pointer text-gray-400 hover:text-red-700 transition" onclick="togglePassword()">&#128064;</span>
          </div>
        </div>
        <div class="flex space-x-4 mt-4">
          <button type="submit" class="flex-1 bg-gradient-to-r from-red-700 via-red-600 to-red-500 text-white py-2 rounded-xl font-bold shadow-lg hover:scale-105 hover:shadow-2xl transition-all duration-200">Login</button>
          <button type="button" class="flex-1 bg-gray-200 text-gray-700 py-2 rounded-xl font-semibold shadow hover:bg-gray-300 transition" onclick="window.close()">Exit</button>
        </div>
      </form>
      <p class="text-center mt-6 text-gray-600">
        Don't have an account?
        <a href="/signup" class="text-red-700 hover:underline font-semibold">Sign up here</a>
      </p>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gradient-to-r from-red-900 via-red-700 to-gray-200 text-gray-100 text-center py-4 mt-12 border-t shadow-inner">
    <span class="font-semibold">&copy; 2025 BloodLink. All rights reserved.</span>
  </footer>

  <!-- Inline Script -->
  <script>
    function togglePassword() {
      const password = document.getElementById("password");
      password.type = password.type === "password" ? "text" : "password";
    }

    function loginRedirect() {
      const role = document.getElementById("role").value;

      if (role === "donor") {
        window.location.href = "Donor/donor";
      } else if (role === "patient") {
        window.location.href = "Patient/patient";
      } else if (role === "admin") {
        window.location.href = "Admin/admin";
      } else {
        alert("Please select a role.");
      }

      return false;
    }
  </script>
</body>
</html>
